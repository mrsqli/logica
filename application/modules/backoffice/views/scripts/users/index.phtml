<?php
/**
 * Displays all the registered users
 *
 * @category backoffice
 * @package backoffice_views
 * @subpackage backoffice_views_users
 * @copyright company
 */

$config = array();

// messages
$config['pageTitle'] = 'Manage users';
$config['addMessage'] = 'Add new user'; 
$config['emptyMessage'] = 'We don\'t have users registered at this time.';

// column names & indexes
$config['columnNames'] = array(
    'Username',
    'First name',
    'Last name',
    'Groups',
);

$config['columnIndexes'] = array(
    'username',
    'firstname',
    'lastname',
    'groupNames',
);

$config['viewLinkColumn'] = 'username';

// render the default listing
echo $this->partial(
    'partials/default-listing.phtml',
    array(
        'config' => $config,
        'paginator' => $this->paginator,
    )
);